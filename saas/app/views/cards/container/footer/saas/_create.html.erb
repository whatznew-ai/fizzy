<% if Current.account.exceeding_storage_limit? && !Current.identity.staff? %>
  <%= render "cards/container/footer/saas/storage_limit_exceeded" %>
<% else %>
  <%= render "cards/container/footer/create", card: card %>
<% end %>
